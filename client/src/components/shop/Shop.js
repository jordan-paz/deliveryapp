import React, { Fragment } from "react";
import ProductList from "../productList/ProductList";

const Shop = props => {
  return (
    <Fragment>
      <h1>Shop</h1>
      <ProductList />
    </Fragment>
  );
};

export default Shop;
